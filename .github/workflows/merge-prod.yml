name: merge workflow

on:
  pull_request:
    branches:
      - prod

jobs:
  build_and_api-test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install dependencies
        run: yarn
      - name: Build
        run: yarn build
      - name: test
        run: yarn test:api
